const { EmbedBuilder, PermissionsBitField } = require("discord.js");

module.exports = {
    name: "sifre-degistir",
    aliases: ["sifre-degistir"],
    cooldown: 5000,
    run: async (client, message, args) => {
      message.reply(`Bunu yalnızca slash-commands ile yapabilirsiniz. </şifre-değiştir:1116846148066476142>`)
    }
 };

 //By ArX Developers